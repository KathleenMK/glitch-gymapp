{{> menu id="dashboard"}}

<section class="ui segment">
  {{> analytics}}
</section>
<section class="ui segment">
  {{> listgoals}}
  {{> addgoal}}
</section>
<section class="ui segment">
  {{> addassessment}}
  {{> listassessments}}
</section>
